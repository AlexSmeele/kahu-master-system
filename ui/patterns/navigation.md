# Navigation Patterns

- Mobile-first, bottom tab bar for main sections.
- Clear top-level nav aligned to product pillars (e.g. Home, Training, Dog, Health, Services, Settings).
- Use consistent icons and labels from the design system.
