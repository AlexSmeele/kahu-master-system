# Form Patterns

- Use KInput, KSelect, KSwitch, KCheckbox as building blocks.
- Prefer single-column forms with labels above fields.
- Show validation inline near the field, in plain language.
- Use a single primary action button at the bottom, with a clear secondary cancel/back if needed.
